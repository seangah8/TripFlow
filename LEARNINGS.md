# LEARNINGS

Append-only log, one dated entry per session, added by running `/log-learning` at the end of each session (see `CLAUDE.md` Section 9 and `.claude/skills/log-learning.md`).

Each entry has two sections: `Domain/Stack` (tech-stack/domain gotchas) and `Claude Code Usage` (what worked or didn't in the workflow itself).
