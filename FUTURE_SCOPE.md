# TripFlow — Future Scope

> Things intentionally left out of the versioned roadmap entirely. Hotel-anchored clustering and in-app routing are scheduled as v9, opening-hours awareness as v8, and multi-city trips (via a `Vacation` wrapper) as v10 (speculative) — see `BLUE_PRINT.md`. What's left below has no version number.

| Feature | Why deferred |
|---------|-------------|
| Editable / draggable plan | Significant UI complexity; view-only is the right scope for now. |
| Multi-user / shared trips | Requires real-time sync or conflict resolution; single owner (v7) keeps it simple. |
| City-level place caching | Repeated searches re-fetch from Google Places; caching + freshness logic is a future optimization. |
| Notifications / reminders | Out of scope entirely for this project. |
| Mobile app | Web only. |
