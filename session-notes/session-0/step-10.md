# Session 0 — Step 10: LEARNINGS.md

## What was built
- `LEARNINGS.md` at the project root — a short header explaining the file's purpose and format, no entries yet.

## Why these decisions were made
- Per `CLAUDE.md` Section 8's rule ("Never write a step file speculatively or mid-step"), the same logic applies here: the actual learnings from this session aren't finalized until the session is genuinely done (after `/code-review`, per Section 5.5) — writing them now would mean guessing at lessons before the session (including end-to-end verification in Step 11) is even finished.

## Suggested commit title
`docs: add LEARNINGS.md skeleton`
