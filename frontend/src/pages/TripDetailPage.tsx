import type { JSX } from 'react';

// Fleshed out in Step 8 — fetches the trip by id and renders map + day timeline.
export function TripDetailPage(): JSX.Element {
  return <div>TripDetailPage placeholder</div>;
}
