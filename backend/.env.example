# --- Server ---
PORT=3001
CORS_ORIGIN=http://localhost:5173

# --- Database (PostgreSQL / TypeORM) ---
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=changeme
DB_NAME=tripflow

# --- External APIs ---
# Added in Session 1 (Google Places, Anthropic Claude)
